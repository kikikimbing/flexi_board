# FlexiBoard example

Demo app for [`flexi_board`](https://pub.dev/packages/flexi_board).

```bash
cd example
flutter run
```

Long-press a card to drag. Use **One / Tabs / Split / Paged** to switch layouts,
and the sparkle control to toggle custom cards.
